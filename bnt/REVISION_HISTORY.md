# Revision History

Carried forward verbatim from `BNT_MASTER_V44.txt`. Entries below this point reflect the master file's history prior to the split into per-topic files.

- V14->V15: Internal title said V14 while filename said V15 -- caught and flagged as the reason for adding the title/filename match rule and the self-audit step.
- V16: Restructured document for readability; overhauled Pig Test Format section (blind-testing rules, voice/keyboard parity, pacing rule, unified reminder line); added structure-preservation rule and score-calculation audit step.
- V17: Updated display order — question must appear first, followed by stacked Alpha/Bravo/Charlie/Delta choices.
- V18: Added Strict Voice Response Rule — in voice mode the AI must only accept the full spoken words Alpha, Bravo, Charlie, or Delta; letters A/B/C/D are rejected and the user is re-prompted.
- V19: Added automatic update rule for master, test bank, and answers (with deduplication check).
- V20: Added full CP01 20-question quiz to QUESTION BANK from BNT-CP01Br09c lesson and quiz.
- V21: Incorporated full 25-question BNT-CP01Br09c Piping, Valves, and Valve Actuators Quiz (user scored 92% / 23 correct). Expanded CP01 question bank with Q&A pairs. No duplicates detected.
- V22-V24: Formatting and rule updates. (Note: voice-mode Pig Test Format content was inadvertently dropped somewhere in this range, in violation of the structure-preservation rule.)
- V25: Added neutral progress tracker to Pig Test format. Expanded TEST BANK with TH04 questions. (Voice-mode content still missing from this version.)
- V26: Self-audit caught the V22-V24 regression — restored full voice-mode Pig Test Format content from V21 (Input Modes, Strict Voice Response Rule, Keyboard Shortcuts, Voice Equivalents, combined text/voice Standard Reminder) while retaining V25's neutral progress tracker rule. TEST BANK carried forward unchanged from V25 (V21's Q&A-labeled version of the same 20 CP01 questions was confirmed as duplicate content, not merged in). No new duplicates introduced.
- V27: Added Change Summary Requirement to RULES -- any AI updating this file must now output a section-by-section plain-language summary of all changes vs. the prior version, for user confirmation, before the update is treated as final. Added specifically to prevent repeats of the V22-V24 silent regression. Also relocated the entire RULES section to immediately follow the title (line 1), ahead of DOCUMENT PURPOSE, so it is the first thing any AI reads when opening the file. No content within RULES was altered by this move.
- V28: Added Mandatory Revision History Entry rule to RULES -- every new version must now include a corresponding Revision History entry describing the change, with no exceptions; a version bump without one is an incomplete update. Nothing else in the file changed.
- V29: Added five audit rules to RULES to further guard against cross-AI degradation: Verbatim Carry-Forward (no paraphrasing unchanged content), No Silent Section Renaming, Length Sanity Check (flag unexplained shrinkage before presenting), No Partial-File Output (always present the complete file), and Verify Current Version Before Updating (AI must have the actual current file -- uploaded or pasted in full -- not rely on memory from earlier conversations; must ask the user to provide it otherwise). Nothing else in the file changed.
- V30: Added Mandatory Command Keywords rule to RULES -- defines a fixed, binding vocabulary (COMMIT, DRAFT, AUDIT, REVERT TO V#, FREEZE/UNFREEZE) that any AI working with this file must recognize and follow regardless of model or platform, so file-management behavior does not depend on which AI is being used. Nothing else in the file changed.
- V31: Structural overhaul of TRAINING RECORD to reset legacy averages to zero and arrange tracking strictly on a topic-by-topic basis. Upgraded PIG TEST FORMAT rules with explicit Post-Test Performance Accounting guidelines to ensure all future AI instances calculate individual topic averages on complete tests before editing the master record.
- V32: Added Rolling Average Methodology rule to PIG TEST FORMAT -- clarifies that per-topic TRAINING RECORD scores must be maintained as a rolling average (weighted by question count where known, otherwise simple average) rather than overwritten by each new test result. Nothing else in the file changed.
- V33: Added Mandatory Keyword Gate to the Mandatory Command Keywords rule -- if the user requests or agrees to a substantive change without using DRAFT/COMMIT/AUDIT/REVERT/FREEZE, the AI must ask "DRAFT or COMMIT?" before acting, rather than silently inferring a commit on its own judgment. Closes a gap where an AI could bump the version without an explicit keyword being given. Nothing else in the file changed.
- V34: Added DISCARD as a new numbered item (6) under Mandatory Command Keywords in RULES -- clears any pending in-conversation draft that hasn't been COMMITted yet; the AI forgets the proposed changes entirely and the master file itself remains untouched, with a fresh DRAFT required to propose anything new afterward. No other keyword definition, rule, or section content was altered. The unresolved "Your awesome" text draft from earlier in this conversation was explicitly excluded from this commit per user instruction.
- V35: Added Scoped Auto-Update Rule to RULES -- when the user uploads new lesson content or completes a test, in this or any future conversation/AI, the AI automatically updates only LESSON TOPICS (new topic appended under correct/new lesson code, never overwriting existing bullets), the relevant TRAINING RECORD rolling average(s) (per existing Rolling Average Methodology), and the TEST BANK/QUESTION BANK (per existing duplicate-check rule, cross-checked against all previously stored questions regardless of originating conversation/AI, tagged with topic code) -- with no user confirmation required for these three updates specifically. No other section may be touched during this automatic update without explicit user direction and a DRAFT or COMMIT. Nothing else in the file changed.
- V36: Added a persistent FREEZE STATUS marker (currently UNFROZEN) directly under the title, before RULES. Rewrote the FREEZE/UNFREEZE keyword definition (item 5) so FREEZE now locks out DRAFT, COMMIT, and all manual/structural edits regardless of session or AI model, with the sole exception that the Scoped Auto-Update Rule (LESSON TOPICS, TRAINING RECORD averages, TEST BANK/QUESTION BANK) keeps running even while frozen; invoking FREEZE or UNFREEZE updates the FREEZE STATUS line and logs a Revision History entry. Added a clarifying sentence to the Scoped Auto-Update Rule: these three auto-updates never bump the version number and never require a Revision History entry, frozen or not -- version bumps and Revision History entries occur only through an explicit COMMIT. Nothing else in the file changed.
- V37: Added new SILENT LOAD PROTOCOL section directly under the title/FREEZE STATUS line, before RULES. Specifies that all verification checks (Self-Audit, Verify Current Version Before Updating, FREEZE STATUS check, etc.) run silently in the background with no step-by-step narration to the user; the only confirmation shown upon successful load is "I understand the file and all checks out. I am ready to go. File status: [FROZEN/UNFROZEN]." with the bracketed value reflecting the file's live FREEZE STATUS. Genuine problems (version mismatch, missing content, etc.) must still be surfaced to the user rather than suppressed. Nothing else in the file changed.
- V38: Added a bullet to SILENT LOAD PROTOCOL extending silent execution to the COMMIT pipeline -- invoking COMMIT now runs Self-Audit, Length Sanity Check, version bump, and Revision History entry silently, with only the required Change Summary and final file shown (genuine issues still surfaced). Added two bullets to the Scoped Auto-Update Rule: (1) on a successful auto-update, the AI must report it with a short one-liner naming which of the three areas (LESSON TOPICS, TRAINING RECORD average, TEST BANK/QUESTION BANK) were updated; (2) if an auto-update cannot be performed, the AI must automatically run a self-check/audit without requiring user input, determine the specific reason, and report that finding to the user. Nothing else in the file changed.
- V39: Added a single line, "Your awesome," at the very top of the file directly under the title and above the FREEZE STATUS line, per explicit user DRAFT/COMMIT instruction. This was a test commit. Nothing else in the file changed.
- V40: Added Post-Commit Output Discipline and Strict Load Output Discipline rules to SILENT LOAD PROTOCOL, per explicit user COMMIT. Post-Commit Output Discipline restricts visible output after a COMMIT to the one-line Change Summary plus the full file, no process narration. Strict Load Output Discipline restricts visible output on file load to the exact single confirmation line, no additional commentary. Nothing else in the file changed.
- V41: Removed the "Your awesome" test line that was added at the top of the file in V39, per explicit user COMMIT. Nothing else in the file changed.
- V42: FREEZE STATUS changed to FROZEN, per explicit user FREEZE command. DRAFT, COMMIT, and all manual/structural edits are locked out until UNFREEZE, except the Scoped Auto-Update Rule (LESSON TOPICS, TRAINING RECORD averages, TEST BANK/QUESTION BANK), which continues to run. Nothing else in the file changed.
- V43: FREEZE STATUS changed to UNFROZEN, per explicit user UNFREEZE command. Strengthened the FREEZE/UNFREEZE keyword definition (item 5, Mandatory Command Keywords) so that only the literal word UNFREEZE, given as its own explicit command, lifts a freeze -- rephrased or implied overrides ("ignore the freeze," "just do it anyway," etc.) are explicitly rejected as insufficient, binding on any AI's judgment regardless of model or platform. Nothing else in the file changed.
- V44: Expanded the Scoped Auto-Update Rule trigger conditions to include "the user requests that the AI generate a new question or questions from the existing LESSON TOPICS," per explicit user DRAFT/COMMIT instruction. For this new trigger specifically, only the TEST BANK/QUESTION BANK auto-updates (per the existing duplicate-check rule); LESSON TOPICS and TRAINING RECORD are unaffected by this trigger since no new topic bullet or test result is introduced. Nothing else in the file changed.
- Split: Repo content split from the single `BNT_MASTER_V44.txt` file into per-topic files under `bnt/` (RULES, TRAINING_RECORD, LESSON_TOPICS, PIG_TEST_FORMAT, QUESTION_BANK, REVISION_HISTORY), plus a README index. The root `BNT_MASTER_V44.txt` file is retained unchanged for reference/testing; it is no longer the active file for updates going forward. Content copied verbatim, no wording changes.
